public static class SnapConfigs
{
    // a constant number for the radius of the sticky ball collider.
    // NOTE: remember to adjust the sticky ball radius in the prefab to sync with this value.
    public static readonly float StickyBallRadius = 0.175f;
    public static readonly float PlayerRadius = 0.25f;
    public static readonly int MaxSnapPoints = 6;
}
