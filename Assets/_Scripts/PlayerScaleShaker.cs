using UnityEngine;

/// <summary>
/// shakes the player's scale when a sticky ball is snapped.
/// </summary>
public class PlayerScaleShaker : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
